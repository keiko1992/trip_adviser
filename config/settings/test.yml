s3:
  public:
    path: ':rails_root/public/system/:class/:id/:filename/:style'
    bucket:
  private:
    path: ':rails_root/public/system/:class/:id/:filename/:style'
    bucket:
  common:
    article_image_path:
    article_embedded_image_path:
