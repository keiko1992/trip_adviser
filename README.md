# Trip Adviser
## Setup Instruction
```
$ bundle exec rake db:migrate
$ bundle exec rails s
```

## Testing
You can run rspec with guard. Temporary, it will run without ```Spring```.
```
$ bundle exec guard
```
